// Generated by CoffeeScript 1.8.0
(function() {
  var RSVP, pad, pluralize, _;

  RSVP = require('rsvp');

  _ = require('underscore');

  pluralize = require('pluralize');

  pad = require('pad');

  module.exports = function(data) {
    return new RSVP.Promise(function(resolve) {
      return data.repo.prs({
        per_page: 100,
        state: 'all'
      }, function(error, prs) {
        var maxNumLength;
        if (error != null) {
          console.log(error.message);
        }
        data.prs = _(prs).map(function(pr) {
          return _(pr).pick('body', 'title', 'number');
        });
        console.log(("  " + data.prs.length + " " + (pluralize('PR', data.prs.length)) + " found:").green);
        _(data.prs).each(function(pr) {
          return console.log(("    " + pr.title).grey);
        });
        maxNumLength = _(data.prs).max(function(pr) {
          return pr.number.toString().length;
        }).number.toString().length;
        _(data.prs).each(function(pr) {
          return pr.number = pad(maxNumLength, pr.number, 0);
        });
        return resolve(data);
      });
    });
  };

}).call(this);
