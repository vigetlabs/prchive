// Generated by CoffeeScript 1.9.1
(function() {
  var RSVP, _, fs, prompt;

  RSVP = require('rsvp');

  fs = require('fs');

  prompt = require('prompt');

  _ = require('underscore');

  module.exports = new RSVP.Promise(function(resolve) {
    var data;
    data = {};
    if (fs.existsSync('.config.json')) {
      data.credentials = JSON.parse(fs.readFileSync('.config.json'));
      return resolve(data);
    } else {
      return prompt.get(require('./config_credentials'), function(err, result) {
        data.credentials = result;
        return resolve(data);
      });
    }
  });

}).call(this);
