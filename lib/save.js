// Generated by CoffeeScript 1.8.0
(function() {
  var RSVP, filesize, fs, httpSync, url, _;

  RSVP = require('rsvp');

  _ = require('underscore');

  httpSync = require('http-sync');

  fs = require('fs');

  url = require('url');

  filesize = require('filesize');

  module.exports = function(data) {
    return new RSVP.Promise(function(resolve) {
      console.log("\n  Saving files:".green);
      _(data.prs).each(function(pr) {
        return _(pr.files).each(function(file) {
          var path, req;
          path = "" + data.dirname + "/" + file.name;
          req = httpSync.request({
            protocol: url.parse(file.url).protocol,
            host: url.parse(file.url).host,
            path: url.parse(file.url).pathname
          });
          fs.writeFileSync(path, req.end().body);
          return console.log(("    " + file.name + " (" + (filesize(fs.statSync('./' + path).size)) + ")").grey);
        });
      });
      return resolve(data);
    });
  };

}).call(this);
